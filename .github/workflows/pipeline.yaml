name: Build and deploy

on:
  push:
    branches:
      - main

env:
  DEPLOYMENT_NAME: inventoryms

jobs:
  setup-build-publish:
      name: Setup build and setup-build-publish
      runs-on: ubuntu-latest
      steps:
        - run: echo "Build started "